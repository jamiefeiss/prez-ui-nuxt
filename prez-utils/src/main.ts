// import { setupCounter } from "./store";
// import { setupCounter } from "./service";

document.querySelector<HTMLDivElement>("#app")!.innerHTML = `
  <div>
    <h1>Prez Utils</h1>
  </div>
`;

// setupCounter(document.querySelector<HTMLButtonElement>("#counter")!);
